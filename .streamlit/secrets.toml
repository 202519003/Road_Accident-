DB_HOST     = "aws-1-ap-south-1.pooler.supabase.com"
DB_PORT     = "5432"
DB_NAME     = "postgres"
DB_USER     = "postgres.ourldbbwnndtymlznzlo"
DB_PASSWORD = "your_actual_password_here"
```

👉 Replace `your_actual_password_here` with your real Supabase database password.

**Where to find your password:**
- Go to `supabase.com` → Your Project → **Project Settings** → **Database** → copy the password

---

## STEP 2 — Protect the secrets file

In your project folder, open or create `.gitignore` and add:
```
.streamlit/secrets.toml
